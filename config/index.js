'use strict';

const conservative   = require('./conservative');
const conservativeJp = require('./conservative-jp');
const progressive    = require('./progressive');

module.exports = {
    conservative,
    conservativeJp,
    progressive,
};
